import { tv } from "tailwind-variants";

export const styles = tv({
  slots: {
    root: "bg-[#4CAF50] py-3 px-10 rounded-full self-center mt-4",
    label: "text-white font-bold text-sm uppercase",
  },
});
